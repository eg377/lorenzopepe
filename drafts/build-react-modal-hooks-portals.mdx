import { PostHeader } from "../../Components/Blog/PostHeader";
import { timestampToDate } from "../../utils/timestampToDate";
import { CodeBlock } from "../../Components/Code/CodeBlock";

export const metadata = {
	tags: ["react", "hooks", "typescript", "html", "redux", "apollo client"],
	timestamp: 1587566116,
	title: "Build a react modal with hooks!",
	href: "/build-react-modal-hooks-portals",
	description:
		"🔥 Let's build a modal from scratch, using React Hooks and Portals API!",
};

<PostHeader
	title={metadata.title}
	dateString={timestampToDate(metadata.timestamp)}
/>
